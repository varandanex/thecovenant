# Database connection for Prisma (Local SQLite)
# To switch to PostgreSQL/Supabase later, update this URL and change provider in schema.prisma
DATABASE_URL=file:./dev.db

# Fuente de contenido (file|database). "file" fuerza a usar export JSON aunque haya DB.
# Si pones "database" forzarás lectura desde Prisma siempre que sea válido.
CONTENT_SOURCE=database

# Flag opcional para permitir contenido BD cuando CONTENT_SOURCE no es file.
USE_DATABASE_CONTENT=true

# Flag explícito prioritario: si es true y DATABASE_URL es válida, se usa BD.
ENABLE_DB=true

# Ejemplos:
# (1) Sólo JSON local -> CONTENT_SOURCE=file, ENABLE_DB=false
# (2) Intentar BD si existe -> CONTENT_SOURCE=, USE_DATABASE_CONTENT=true, ENABLE_DB=false
# (3) Forzar BD -> CONTENT_SOURCE=database, ENABLE_DB=true
